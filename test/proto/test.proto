syntax = "proto3";

package test;

service svc {
  rpc successful(test_message) returns (test_message);
  rpc errors(test_message) returns (test_message);
}

message test_message {
  string foo = 1;
}
